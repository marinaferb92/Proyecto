<?php
// categorias_tratamientos.php
require_once 'config.php';
require_once 'auth.php';

require_login();
require_role(['admin']);

$pageTitle = "Categorías de tratamientos";
include 'header.php';

$categorias = $pdo->query("SELECT * FROM CategoriasTratamientos ORDER BY nombre")->fetchAll();
?>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h3 class="mb-0"><i class="bi bi-tags me-2"></i>Categorías de tratamientos</h3>
  <a href="categoria_tratamiento_editar.php" class="btn btn-primary">
    <i class="bi bi-plus-circle"></i> Nueva categoría
  </a>
</div>

<div class="card shadow-sm">
  <div class="table-responsive">
    <table class="table table-hover mb-0">
      <thead class="table-light">
        <tr>
          <th>Nombre</th>
          <th>Descripción</th>
          <th class="text-end">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <?php if ($categorias): ?>
          <?php foreach ($categorias as $c): ?>
            <tr>
              <td><?= htmlspecialchars($c['nombre']) ?></td>
              <td><?= htmlspecialchars($c['descripcion']) ?></td>
              <td class="text-end">
                <a href="categoria_tratamiento_editar.php?id=<?= $c['categoria_id'] ?>"
                   class="btn btn-sm btn-outline-primary">
                  <i class="bi bi-pencil"></i>
                </a>
              </td>
            </tr>
          <?php endforeach; ?>
        <?php else: ?>
          <tr>
            <td colspan="3" class="text-center text-muted py-3">
              No hay categorías definidas.
            </td>
          </tr>
        <?php endif; ?>
      </tbody>
    </table>
  </div>
</div>

<?php include 'footer.php'; ?>
