<ul class="navbar-nav me-auto mb-2 mb-lg-0">

    <?php if ($_SESSION['rol'] === 'admin' || $_SESSION['rol'] === 'recepcionista'): ?>
        <li class="nav-item">
            <a class="nav-link" href="clientes.php">Clientes</a>
        </li>
    <?php endif; ?>

    <?php if ($_SESSION['rol'] === 'admin' || $_SESSION['rol'] === 'medico' || $_SESSION['rol'] === 'recepcionista'): ?>
        <li class="nav-item">
            <a class="nav-link" href="citas.php">Citas</a>
        </li>
    <?php endif; ?>

    <?php if ($_SESSION['rol'] === 'admin'): ?>
        <li class="nav-item">
            <a class="nav-link" href="empleados.php">Empleados</a>
        </li>

        <li class="nav-item">
            <a class="nav-link" href="tratamientos.php">Tratamientos</a>
        </li>

        <li class="nav-item">
            <a class="nav-link" href="inventario.php">Inventario</a>
        </li>
    <?php endif; ?>

</ul>

<ul class="navbar-nav">
    <li class="nav-item">
        <span class="navbar-text me-3">
            Hola, {{ '{{ $_SESSION["usuario"] }}' }}
        </span>
    </li>
    <li class="nav-item">
        <a class="nav-link text-warning" href="logout.php">Salir</a>
    </li>
</ul>
