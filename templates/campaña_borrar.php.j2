<?php
require_once 'config.php';
require_once 'auth.php';

require_login();
require_role(['admin']);

$id = $_GET['id'] ?? null;

if ($id) {
    $stmt = $pdo->prepare("DELETE FROM CampañasMarketing WHERE campaña_id=:id");
    $stmt->execute([':id'=>$id]);
}

header("Location: campañas.php");
exit;
?>
